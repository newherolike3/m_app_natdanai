import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'm_app_natdanai',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
